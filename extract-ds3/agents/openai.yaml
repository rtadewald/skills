interface:
  display_name: "Extract DS 3"
  short_description: "Copy a website into an approval-gated design system"
  default_prompt: "Use $extract-ds3 to classify this site's runtime, copy it into a design system, present Overview for approval, then build Components."
policy:
  allow_implicit_invocation: false
