interface:
  display_name: "Extract DS"
  short_description: "Fast copy-first website design system workflow"
  default_prompt: "Use $extract-ds to take the fastest copy-first path: classify the runtime, present Overview for approval, then build source-backed Components."
policy:
  allow_implicit_invocation: false
