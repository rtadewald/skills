interface:
  display_name: "Img to HTML"
  short_description: "Mock de UI → HTML/CSS/JS estático em etapas com aprovação"
  default_prompt: "Use $img-to-html: a partir desta imagem de mock, rode o fluxo completo (wireframe → bg → nav + cards → assets gpt2 → fontes) gerando index.html + assets/styles.css, parando para aprovação em cada etapa."
policy:
  allow_implicit_invocation: false
