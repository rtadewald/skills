interface:
  display_name: "Img to HTML"
  short_description: "Mock → measure/OCR → HTML (vocab fechado) → render/diff 2–3×; assets OpenRouter"
  default_prompt: "Use $img-to-html: measure.py → plan/slots → type+images → compile (vocab) → render/diff loop em design-systems/<slug>/."
policy:
  allow_implicit_invocation: false
