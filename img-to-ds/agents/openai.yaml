interface:
  display_name: "Img → DS"
  short_description: "Mock image → design-systems/<slug>/design-system.html"
  default_prompt: "Use $img-to-ds: analise esta imagem, escolha um slug e gere design-systems/<slug>/ (design-system.html + assets + STACK.md)."
policy:
  allow_implicit_invocation: false
