interface:
  display_name: "Extract Design System"
  short_description: "HTML de referência → design-system.html (pattern library viva)"
  default_prompt: "Use $extract-design-system: a partir deste HTML de referência, gere o design-system.html."
policy:
  allow_implicit_invocation: false
